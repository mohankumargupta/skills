# SKILL: Git management

After running ADD_DEVICE.md sub-skill, 
do: 

1. a git commit with commit message "add device <device> to espforge" 
2. a git diff between the current feature branch and main and save it as `<device>_diff.diff`
   in the working directory.
 

