# Crate Suitability Standards

A crate is considered suitable for this workflow if it meets the following:
1. **embedded-hal v1** dependency: The `Cargo.toml` must specify a requirement compatible with version 1.x (`^1`, `>=1`, `~1`, or `1`).
